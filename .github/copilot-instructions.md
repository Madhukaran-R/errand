# Workspace Notes

- This project is a Next.js app configured for static export.
- Keep `output: "export"` in `next.config.mjs`.
- Keep `images.unoptimized: true` for static hosting compatibility.
- Build output is generated in the `out/` directory.
